# Testing

```bash
cargo test
```

Covers types, stats, moves, instances, battle engine, encounters, ASCII, saves, data adapters, and integration scenarios.

TUI is manual: adventure starter → wild → center → save; classic PvC; species dex; resize mid-battle.
